// Staff login page gets its own full-screen layout, bypassing the AdminSidebar layout
export default function StaffLoginLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
